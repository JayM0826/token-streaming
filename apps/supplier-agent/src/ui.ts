export function renderSupplierAgentUi(nonce: string): string {
  return `<!doctype html>
<html lang="zh-CN"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><meta name="referrer" content="no-referrer"><title>共算云供应客户端</title>
<style nonce="${nonce}">
:root{--ink:#14251e;--muted:#687970;--paper:#f2f5ef;--panel:#fff;--line:#dce5db;--deep:#10261d;--lime:#b9f34a;--danger:#b94d4d}*{box-sizing:border-box}body{margin:0;color:var(--ink);background:var(--paper);font-family:Inter,"PingFang SC","Microsoft YaHei",system-ui,sans-serif}.shell{min-height:100vh;display:grid;grid-template-columns:240px 1fr}.side{display:flex;flex-direction:column;padding:28px 22px;color:white;background:var(--deep)}.brand{display:flex;align-items:center;gap:10px;font-weight:900}.mark{width:31px;height:31px;display:grid;place-items:center;border-radius:9px;color:var(--deep);background:var(--lime)}.side p{margin:38px 0 8px;color:#c8d6ce;font-size:13px;line-height:1.7}.side small{color:#789087;font-size:10px;line-height:1.7}.guard{margin-top:auto;padding:15px;border:1px solid #29483b;border-radius:13px;background:#173429}.guard b,.guard span{display:block}.guard b{color:var(--lime);font-size:11px}.guard span{margin-top:6px;color:#8fa59a;font-size:9px;line-height:1.6}.main{padding:34px}.top{display:flex;align-items:center;justify-content:space-between;gap:20px;margin-bottom:25px}.top h1{margin:6px 0;font-size:34px;letter-spacing:-.04em}.top p{margin:0;color:var(--muted);font-size:12px}.badge{padding:8px 11px;border-radius:99px;color:#53685d;background:#e7ece5;font-size:10px;font-weight:800}.badge.online{color:#3d6e29;background:#e7f7d6}.panel{max-width:980px;margin:0 auto 15px;padding:22px;border:1px solid var(--line);border-radius:16px;background:var(--panel);box-shadow:0 14px 45px rgba(20,45,33,.06)}.panel h2{margin:4px 0 7px;font-size:19px}.panel>p{margin:0 0 20px;color:var(--muted);font-size:11px;line-height:1.7}.kicker{color:#70867a;font-size:9px;font-weight:900;letter-spacing:.13em}.grid{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:12px}.grid.four{grid-template-columns:repeat(4,minmax(0,1fr))}label{display:grid;gap:6px;margin-bottom:13px;color:#50655a;font-size:10px;font-weight:800}input,select{width:100%;min-height:41px;padding:9px 11px;border:1px solid #d6dfd7;border-radius:9px;outline:0;background:#fbfcfa}input:focus,select:focus{border-color:#8db95b;box-shadow:0 0 0 3px rgba(146,215,52,.12)}.checks{display:flex;gap:18px;align-items:center;min-height:41px}.checks label{display:flex;align-items:center;gap:6px;margin:0}.checks input{width:auto;min-height:auto}.note{padding:12px;border:1px solid #e0e9d8;border-radius:10px;color:#607168;background:#f6faef;font-size:9px;line-height:1.7}.actions{display:flex;justify-content:flex-end;gap:9px;margin-top:18px}.primary,.secondary,.danger{padding:11px 16px;border-radius:10px;cursor:pointer;font-weight:850}.primary{border:0;background:var(--lime)}.secondary{border:1px solid var(--line);background:white}.danger{border:1px solid #efd0d0;color:var(--danger);background:#fffafa}button:disabled{cursor:not-allowed;opacity:.5}.metrics{display:grid;grid-template-columns:repeat(4,1fr);gap:11px;margin-bottom:15px}.metric{padding:17px;border:1px solid var(--line);border-radius:13px;background:white}.metric span{display:block;color:var(--muted);font-size:9px}.metric b{display:block;margin-top:10px;font-size:23px}.details{display:grid;grid-template-columns:repeat(2,1fr);gap:0;margin:12px 0}.details div{padding:12px;border-bottom:1px solid #edf1ec}.details dt{color:var(--muted);font-size:9px}.details dd{margin:5px 0 0;overflow:hidden;font-size:11px;font-weight:750;text-overflow:ellipsis;white-space:nowrap}.secret{padding:12px;border-radius:9px;color:#dce9e1;background:var(--deep);font:10px/1.6 ui-monospace,Consolas,monospace;word-break:break-all}.hidden{display:none!important}.toast{position:fixed;right:24px;bottom:24px;max-width:420px;padding:13px 16px;border-radius:11px;color:white;background:var(--deep);box-shadow:0 12px 35px rgba(0,0,0,.2);font-size:11px}.toast.error{background:#7e3030}@media(max-width:800px){.shell{display:block}.side{min-height:auto}.side p,.side small,.guard{display:none}.main{padding:20px 14px}.top{align-items:flex-start;flex-direction:column}.grid,.grid.four,.metrics,.details{grid-template-columns:1fr}.panel{padding:17px}}
</style></head><body><div class="shell"><aside class="side"><div class="brand"><span class="mark">共</span>共算云供应端</div><p>把已授权的模型容量安全接入市场。</p><small>节点内核与管理界面分离。客户请求不会进入本地 Codex、Shell 或文件系统。</small><div class="guard"><b>本地安全边界</b><span>回环管理接口 · 加密密钥库 · 会话令牌 · v3 签名凭证</span></div></aside><main class="main"><header class="top"><div><span class="kicker">SUPPLIER AGENT</span><h1>供应控制中心</h1><p id="subtitle">正在读取本地节点状态……</p></div><span class="badge" id="stateBadge">连接中</span></header>
<section class="panel hidden" id="setupPanel"><span class="kicker">FIRST-RUN SETUP</span><h2>配置你的供应节点</h2><p>凭据只在本机使用，并由你的口令加密保存。大文件任务由客户端主动连接平台领取，不需要把文件系统或管理端口暴露到公网。</p><form id="setupForm"><div class="grid"><label>Provider 标识<input name="providerId" value="openai" required pattern="[A-Za-z0-9][A-Za-z0-9._:-]{1,127}"></label><label>精确模型名（逗号分隔）<input name="models" placeholder="gpt-5.4-2026-08-01" required></label></div><div class="grid"><label>上游协议<select name="upstreamProtocol"><option value="responses">Responses API</option><option value="chat-completions">Chat Completions</option></select></label><label>上游 HTTPS Base URL<input name="upstreamBaseUrl" type="url" value="https://api.openai.com/v1" required></label></div><div class="grid"><label>上游主机白名单<input name="upstreamHosts" value="api.openai.com" required></label><label>公网节点地址（实时短请求）<input name="publicEndpoint" type="url" placeholder="https://node.example.com/v3/inference" required></label></div><div class="grid"><label>共算云平台地址<input name="controlPlaneBaseUrl" type="url" value="https://gongsuanyun-market.wenzaiyin.chatgpt.site" required></label><label>允许数据等级<span class="checks"><label><input type="checkbox" name="p0" checked>P0</label><label><input type="checkbox" name="p1">P1</label></span></label></div><div class="grid"><label>Provider API Key<input name="apiKey" type="password" autocomplete="off" minlength="8" required></label><label>本地加密口令<input name="passphrase" type="password" autocomplete="new-password" minlength="12" required></label></div><div class="grid"><label>网关令牌（留空自动生成）<input name="gatewayToken" type="password" autocomplete="off" minlength="32"></label><label>大文件任务上限（MiB，0 为关闭）<input name="maxArtifactMiB" type="number" value="256" min="0" max="256"></label></div><div class="grid four"><label>本地端口<input name="gatewayPort" type="number" value="8789" min="1" max="65535"></label><label>每分钟请求<input name="rpm" type="number" value="30" min="1"></label><label>每分钟 Token<input name="tpm" type="number" value="100000" min="1"></label><label>并发<input name="concurrency" type="number" value="2" min="1"></label></div><div class="grid"><label>最大输出 Token<input name="maxOutput" type="number" value="4096" min="1" max="32768"></label><label>单次上游输入字节<input name="maxInput" type="number" value="262144" min="20480" max="1000000"></label></div><div class="grid"><label>文件处理分段（KiB）<input name="artifactSegmentKiB" type="number" value="128" min="16" max="512"></label><label>处理方式<input value="只读分段 → 模型处理 → 分层汇总" disabled></label></div><div class="note">当前文件任务只接受 UTF-8 文本、Markdown、CSV、JSON、NDJSON 与 XML；不解压、不执行文件内容，也不会把客户文件写入普通工作目录。请只接入条款明确允许托管推理或转售的 Provider Project、商业账户或自托管许可。</div><div class="actions"><button class="primary">加密保存并启动</button></div></form></section>
<section class="panel hidden" id="unlockPanel"><span class="kicker">LOCAL VAULT</span><h2>解锁供应节点</h2><p>配置已保存在本机。输入加密口令后才会在内存中加载 Provider 凭据并启动节点。</p><form id="unlockForm"><label>本地加密口令<input name="passphrase" type="password" autocomplete="current-password" minlength="12" required></label><div class="actions"><button class="primary">解锁并上线</button></div></form></section>
<section class="hidden" id="dashboardPanel"><div class="metrics"><article class="metric"><span>已完成任务</span><b id="completedJobs">0</b></article><article class="metric"><span>处理 Token</span><b id="totalTokens">0</b></article><article class="metric"><span>失败任务</span><b id="failedJobs">0</b></article><article class="metric"><span>文件任务</span><b id="artifactState">停止</b></article></div><article class="panel"><span class="kicker">LIVE NODE</span><h2>节点已在线</h2><p>实时短请求使用已审核的 HTTPS 网关；大文件任务由客户端主动向平台轮询、分块读取并发送检查点，本地管理页不会暴露到公网。</p><dl class="details"><div><dt>Provider</dt><dd id="providerId">—</dd></div><div><dt>精确模型</dt><dd id="models">—</dd></div><div><dt>公网地址</dt><dd id="endpoint">—</dd></div><div><dt>本地端口</dt><dd id="gatewayPort">—</dd></div><div><dt>文件任务编号</dt><dd id="artifactTask">等待调度</dd></div><div><dt>文件处理进度</dt><dd id="artifactProgress">0 段 · 0 B</dd></div></dl><div class="actions"><a class="secondary" href="https://gongsuanyun-market.wenzaiyin.chatgpt.site/" target="_blank" rel="noreferrer">打开平台账本</a><button class="secondary" id="revealButton">查看接入资料</button><button class="danger" id="lockButton">安全下线并锁定</button></div></article></section>
<section class="panel hidden" id="connectionPanel"><span class="kicker">MARKETPLACE CONNECTION</span><h2>平台接入资料</h2><p>网关令牌属于高敏感凭据。每次查看都必须重新验证本地加密口令；Provider API Key 永远不会显示或提交给平台。</p><form id="connectionUnlockForm"><label>重新输入本地加密口令<input name="passphrase" type="password" autocomplete="current-password" minlength="12" required></label><div class="actions"><button type="button" class="secondary" id="cancelConnection">取消</button><button class="primary">验证并查看</button></div></form><div class="hidden" id="connectionDetails"><dl class="details"><div><dt>Provider</dt><dd id="connectProvider">—</dd></div><div><dt>模型范围</dt><dd id="connectModels">—</dd></div><div><dt>网关地址</dt><dd id="connectEndpoint">—</dd></div><div><dt>任务平台</dt><dd id="connectControlPlane">—</dd></div><div><dt>数据范围</dt><dd id="connectData">—</dd></div></dl><div class="secret" id="connectToken">—</div><div class="actions"><button class="secondary" id="copyButton">复制接入资料</button><button class="primary" id="closeConnection">完成</button></div></div></section></main></div><div class="toast hidden" id="toast"></div>
<script nonce="${nonce}">
(()=>{
  let launchBootstrap=new URLSearchParams(location.hash.slice(1)).get('bootstrap');
  history.replaceState(null,'',location.pathname);
  const $=id=>document.getElementById(id);
  const panels=['setupPanel','unlockPanel','dashboardPanel'];
  let latestStatus=null;
  let latestConnection=null;
  let revealTimer=null;
  const toast=(message,error=false)=>{const el=$('toast');el.textContent=message;el.className='toast'+(error?' error':'');setTimeout(()=>el.className='toast hidden',4000)};
  const api=async(path,options={})=>{const response=await fetch(path,{...options,credentials:'same-origin',headers:{'content-type':'application/json',...options.headers}});const body=await response.json();if(!response.ok)throw new Error(body.error?.message||'本地客户端请求失败');return body};
  const show=id=>{for(const panel of panels)$(panel).classList.toggle('hidden',panel!==id)};
  const bytes=value=>value>=1048576?(value/1048576).toFixed(1)+' MiB':value>=1024?(value/1024).toFixed(1)+' KiB':value+' B';
  const workerLabel=state=>({stopped:'停止',polling:'等待',processing:'处理中',error:'异常'})[state]||state;
  const render=status=>{latestStatus=status;$('stateBadge').textContent=status.nodeStatus==='online'?'在线供给':status.configured?'已锁定':'未配置';$('stateBadge').className='badge'+(status.nodeStatus==='online'?' online':'');$('subtitle').textContent=status.nodeStatus==='online'?'节点运行中，等待平台安全调度。':status.configured?'密钥库已锁定，当前不会接收新任务。':'完成一次配置即可开始供给。';if(!status.configured)show('setupPanel');else if(!status.unlocked)show('unlockPanel');else{show('dashboardPanel');$('completedJobs').textContent=status.metrics.completedJobs.toLocaleString();$('totalTokens').textContent=status.metrics.totalTokens.toLocaleString();$('failedJobs').textContent=status.metrics.failedJobs.toLocaleString();$('artifactState').textContent=workerLabel(status.artifactWorker.state);$('providerId').textContent=status.providerId;$('models').textContent=status.models.join(', ');$('endpoint').textContent=status.publicGatewayEndpoint;$('gatewayPort').textContent='127.0.0.1:'+status.gatewayPort;$('artifactTask').textContent=status.artifactWorker.taskId||'等待调度';$('artifactProgress').textContent=status.artifactWorker.completedSegments+'/'+(status.artifactWorker.totalSegments??'—')+' 段 · '+bytes(status.artifactWorker.processedBytes)+(status.artifactWorker.lastErrorCode?' · '+status.artifactWorker.lastErrorCode:'')}};
  const refresh=async()=>{try{render(await api('/api/status'))}catch(error){toast(error.message,true)}};
  const closeConnection=()=>{if(revealTimer)clearTimeout(revealTimer);revealTimer=null;latestConnection=null;$('connectToken').textContent='—';$('connectionDetails').classList.add('hidden');$('connectionUnlockForm').classList.remove('hidden');$('connectionUnlockForm').reset();$('connectionPanel').classList.add('hidden')};
  const showConnection=connection=>{latestConnection=connection;$('connectProvider').textContent=connection.providerId;$('connectModels').textContent=connection.exactModels.join(', ');$('connectEndpoint').textContent=connection.gatewayEndpoint;$('connectControlPlane').textContent=connection.controlPlaneBaseUrl;$('connectData').textContent=connection.dataClasses.join(' / ');$('connectToken').textContent=connection.gatewayBearerToken;$('connectionUnlockForm').classList.add('hidden');$('connectionDetails').classList.remove('hidden');$('connectionPanel').classList.remove('hidden');$('connectionPanel').scrollIntoView({behavior:'smooth'});if(revealTimer)clearTimeout(revealTimer);revealTimer=setTimeout(closeConnection,60000)};
  $('setupForm').addEventListener('submit',async event=>{event.preventDefault();const data=new FormData(event.currentTarget);const split=name=>String(data.get(name)||'').split(',').map(value=>value.trim()).filter(Boolean);try{const connection=await api('/api/setup',{method:'POST',body:JSON.stringify({profile:{providerId:String(data.get('providerId')),allowedModels:split('models'),allowedDataClasses:[data.get('p0')?'P0':null,data.get('p1')?'P1':null].filter(Boolean),publicGatewayEndpoint:String(data.get('publicEndpoint')),controlPlaneBaseUrl:String(data.get('controlPlaneBaseUrl')),gatewayPort:Number(data.get('gatewayPort')),upstreamProtocol:String(data.get('upstreamProtocol')),upstreamBaseUrl:String(data.get('upstreamBaseUrl')),upstreamHostAllowlist:split('upstreamHosts'),limits:{requestsPerMinute:Number(data.get('rpm')),tokensPerMinute:Number(data.get('tpm')),concurrency:Number(data.get('concurrency')),maxOutputTokens:Number(data.get('maxOutput')),maxInputBytes:Number(data.get('maxInput')),maxArtifactBytes:Number(data.get('maxArtifactMiB'))*1048576,artifactSegmentBytes:Number(data.get('artifactSegmentKiB'))*1024}},upstreamApiKey:String(data.get('apiKey')),gatewayToken:String(data.get('gatewayToken')),passphrase:String(data.get('passphrase'))})});event.currentTarget.reset();showConnection(connection);await refresh();toast('节点已加密保存并启动')}catch(error){toast(error.message,true)}});
  $('unlockForm').addEventListener('submit',async event=>{event.preventDefault();const data=new FormData(event.currentTarget);try{await api('/api/unlock',{method:'POST',body:JSON.stringify({passphrase:String(data.get('passphrase'))})});event.currentTarget.reset();await refresh();toast('密钥库已解锁，节点在线')}catch(error){toast(error.message,true)}});
  $('lockButton').addEventListener('click',async()=>{try{closeConnection();await api('/api/lock',{method:'POST',body:'{}'});await refresh();toast('节点已安全下线并锁定')}catch(error){toast(error.message,true)}});
  $('revealButton').addEventListener('click',()=>{$('connectionPanel').classList.remove('hidden');$('connectionUnlockForm').classList.remove('hidden');$('connectionDetails').classList.add('hidden');$('connectionPanel').scrollIntoView({behavior:'smooth'});$('connectionUnlockForm').elements.passphrase.focus()});
  $('connectionUnlockForm').addEventListener('submit',async event=>{event.preventDefault();const data=new FormData(event.currentTarget);try{showConnection(await api('/api/connection',{method:'POST',body:JSON.stringify({passphrase:String(data.get('passphrase'))})}));event.currentTarget.reset()}catch(error){toast(error.message,true)}});
  $('cancelConnection').addEventListener('click',closeConnection);
  $('closeConnection').addEventListener('click',closeConnection);
  $('copyButton').addEventListener('click',async()=>{if(!latestConnection)return;const text=['Provider: '+latestConnection.providerId,'Models: '+latestConnection.exactModels.join(','),'Gateway: '+latestConnection.gatewayEndpoint,'Control plane: '+latestConnection.controlPlaneBaseUrl,'Gateway token: '+latestConnection.gatewayBearerToken].join('\n');await navigator.clipboard.writeText(text);toast('接入资料已复制，请仅粘贴到共算云授权页')});
  const initialize=async()=>{try{if(launchBootstrap){const bootstrapToken=launchBootstrap;launchBootstrap=null;await api('/api/bootstrap',{method:'POST',body:JSON.stringify({bootstrapToken})})}await refresh()}catch(error){launchBootstrap=null;toast(error.message,true)}};
  initialize();
  setInterval(()=>{if(latestStatus?.unlocked)refresh()},3000);
})();
</script></body></html>`;
}
